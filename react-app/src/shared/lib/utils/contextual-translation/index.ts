/**
 * @module ContextualTranslationIndex
 * Индексный файл для экспорта утилит контекстного перевода.
 * Предоставляет единую точку доступа к функциям работы с контекстным переводом.
 */

export {
  shouldUseContextualTranslation,
  getContextualTranslationConfig,
  validateContextualTranslationParams,
} from './contextual-translation';
